apiVersion: argocd-image-updater.argoproj.io/v1alpha1
kind: Application
metadata:
  name: rielu-site
  namespace: argocd
spec:
  namespace: argocd
  applicationRefs:
    - namePattern: "rielu-site"
      images:
        - alias: "rielu-site"
          imageName: "docker.io/rieluinfra/rielu-site"
          commonUpdateSettings:
            updateStrategy: "semver"
